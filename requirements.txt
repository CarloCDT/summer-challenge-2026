gymnasium>=0.29.0
numpy>=1.24.0
pygame>=2.2.0
matplotlib>=3.5.0
jupyter>=1.0.0

# Training stack (see README.md). torch needs a CUDA
# build to use a GPU - the generic `torch` PyPI wheel is CPU-only; install from
# https://download.pytorch.org/whl/<cuXXX> matching your driver's CUDA version instead,
# e.g.: pip install torch --index-url https://download.pytorch.org/whl/cu130
torch>=2.5.0
tensorboard>=2.18.0
pyyaml>=6.0
